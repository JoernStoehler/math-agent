%\begin{prob}
%Wir untersuchen ein Rohr darauf, ob sein Querschnitt ein Kreis ist. Dazu messen wir in jeder Richtung die Breite des Rohrs. Wir stellen fest, dass die Breite an jedem Punkt $\varphi\in\alpha([0,2\pi))$ gleich ist.

%(a) Es sei jetzt der Querschnitt gegeben durch die geschlossene, zweimal stetig differenzierbare, konvexe Kurve $\alpha:[0,1]\to\C$, so dass $\alpha'(t)=-\lambda(t)\alpha'(t+1/2)$, wobei $\lambda(t)>0$ f"ur alle $t\in[0,1]$. Zeige, dass dann $\|\alpha(t)-\alpha(t+1/2)\|$ konstant ist und folgere, dass $\alpha(t)$ eine Kreiskurve ist. Betrachte dazu die Kurve $\beta(t):=\alpha(t)-\alpha(t+1/2)$.

%(b) Finde eine stückweise stetig differenzierbare, aber nicht überall stetig differenzierbare Kurve $\gamma:[0,1]\to\C$, f"ur die $\gamma(t)-\gamma(t+1/2)$ konstant ist.
%\end{prob}
